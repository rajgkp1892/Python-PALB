class Solution:
    def twoSum(self, nums, target):
        mp = {}
        
        for i in range(len(nums)):
            required = target - nums[i]
            
            if required in mp:
                return [mp[required], i]
            
            mp[nums[i]] = i

        
